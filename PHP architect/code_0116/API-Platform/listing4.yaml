# FosHttpCache
fos_http_cache:
    cache_control:
        rules:
            -
                match:
                    path: ^/$
                headers:
                    cache_control:
                        public: false
                        max_age: 60
                    etag: true